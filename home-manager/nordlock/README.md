# Nordlock

`Super+X` runs this secure Quickshell lock screen. It uses the compositor's
session-lock protocol and the NixOS `quickshell-lock` PAM service.

Retheme it by editing the named values in `Theme.qml`. The background is the
static `#2E3440` color.
